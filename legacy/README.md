# Archivo histórico

Estos archivos conservan el código de demostración y el agente anteriores a la versión 0.2.0. No forman parte de las pruebas ni del entrenamiento activo. Sus afirmaciones sobre AGI y sus indicadores preasignados de éxito no constituyen evidencia experimental. Sus imports corresponden a la estructura anterior y no se mantienen como interfaz ejecutable.

Usa `run_rsi.py`, `benchmarks/run_benchmarks.py` y `tests/` desde la raíz del proyecto actualizado.
